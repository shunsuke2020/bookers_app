<h1>Editing Book</h1>
<%= form_for(@book, url:"/todolists/#{@book.id}") do |f| %>
    <label>title</label>
    <%= f.text_field :title %>
     <label>body</label>
    <%= f.text_area :body %>
    <%= f.submit 'update book' %>    
<% end %>